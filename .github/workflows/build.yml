name: Build GameServer Only

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1

    - name: Create Fake StructParser
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path "Utilities/bin"
        Set-Content -Path "Utilities/bin/StructParser.exe" -Value ([byte[]](0x4D,0x5A)) -Encoding Byte

    - name: Build GameServer
      shell: cmd
      run: msbuild CoreGameServer.sln /t:CoreGameServer /p:Configuration=Debug /p:Platform=Win32 /m

    - name: Upload GameServer
      uses: actions/upload-artifact@v4
      with:
        name: GameServer
        path: Core/bin/Debug/GameServer.exe
        if-no-files-found: error
