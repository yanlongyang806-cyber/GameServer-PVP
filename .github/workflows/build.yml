name: Build GameServer Only

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1

<<<<<<< HEAD
    - name: Create Fake StructParser
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path "Utilities/bin"
        Set-Content -Path "Utilities/bin/StructParser.exe" -Value ([byte[]](0x4D,0x5A)) -Encoding Byte

    - name: Build GameServer
      shell: cmd
      run: msbuild CoreGameServer.sln /t:CoreGameServer /p:Configuration=Debug /p:Platform=Win32 /m

    - name: Upload GameServer
=======
    - name: Create Fake StructParser (disable all structparser calls)
      run: |
        mkdir Utilities\bin -Force
        echo. > Utilities\bin\StructParser.exe

    - name: Build GameServer
      shell: cmd
      run: msbuild CoreGameServer.sln /t:CoreGameServer /p:Configuration=Debug /p:Platform=Win32 /p:PreBuildEvent="" /m /v:minimal

    - name: Upload GameServer.exe
>>>>>>> 073c30e (绠€鍖栨瀯寤烘祦绋嬶細绂佺敤 StructParser锛岀洿鎺ョ紪璇?GameServer)
      uses: actions/upload-artifact@v4
      with:
        name: GameServer
        path: Core/bin/Debug/GameServer.exe
        if-no-files-found: error
